{
  "name": "vibesdk-sandbox-contract",
  "private": true,
  "version": "0.0.1",
  "description": "Ported types (worker/services/sandbox/sandboxTypes.ts) and target client interface (worker/services/sandbox/BaseSandboxService.ts's abstract method surface) for the AWS sandbox replacement -- contract only, no implementation. Not part of the main vibesdk build.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --emitDeclarationOnly --outDir dist && esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts --root ."
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}
