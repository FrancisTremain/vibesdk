{
  "name": "vibesdk-model-config-defaults",
  "private": true,
  "version": "0.0.1",
  "description": "Duplicated snapshot of worker/agents/inferutils/config.ts + config.types.ts (AGENT_CONFIG/AGENT_CONSTRAINTS, the AI model catalog) and the pure merge/constraint/BYOK logic from worker/database/services/ModelConfigService.ts and worker/api/controllers/modelConfig/. Not part of the main vibesdk build.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --emitDeclarationOnly --outDir dist && esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts --root ."
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}
