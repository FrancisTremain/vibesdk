{
  "name": "vibesdk-auth-crypto",
  "private": true,
  "version": "0.0.1",
  "description": "Port of PasswordService (worker/utils/passwordService.ts) -- password hashing/verification/strength validation. Pure crypto/logic, no AWS SDK dependency. Not part of the main vibesdk build.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts --root ."
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}
