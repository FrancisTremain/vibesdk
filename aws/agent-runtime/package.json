{
  "name": "vibesdk-agent-runtime",
  "private": true,
  "version": "0.0.1",
  "description": "The real actor-model session runtime built on aws/actor-spike's proven Lambda-per-WebSocket-message pattern (docs/aws-migration-technical-design.md's 'actor-model gap' section) -- session lifecycle, connection routing, and conversation-state message handling for the AWS replacement of worker/agents/core/codingAgent.ts. Not part of the main vibesdk build.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --outfile=dist/handler.js --external:@aws-sdk/*",
    "package": "npm run build && cd dist && zip -r ../agent-runtime.zip handler.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts --root ."
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.138",
    "@types/node": "^20.14.0",
    "aws-sdk-client-mock": "^4.1.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}
